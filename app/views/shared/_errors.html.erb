<% if obj.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(obj.errors.count, "error") %> prohibited this organization from being saved:</h2>

      <ul>
        <% obj.errors.full_messages.each do |message| %>
            <li style="color: red"><%= message %></li>
        <% end %>
      </ul>
    </div>
<% end %>